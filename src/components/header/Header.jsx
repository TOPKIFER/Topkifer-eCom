import React from "react";
import "./Header.scss";
import Navbar from "./navbar/Navbar";

const Header = () => {
    return (
        <header className="Header">
            <Navbar/>
        </header>
    );
}

export default Header
